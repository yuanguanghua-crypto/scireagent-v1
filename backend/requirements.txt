# Core
Django==5.1.3
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-ckeditor-5==0.2.17

# Database
psycopg2-binary==2.9.9
dj-database-url==2.3.0

# Server
gunicorn==22.0.0
whitenoise==6.7.0

# Image/PDF Processing
Pillow==11.1.0
PyMuPDF==1.25.4
reportlab==5.0.0

# Email (Outbox + cron send via django-anymail; secrets via env, not committed)
django-anymail==10.3

# Utilities
python-dotenv==1.0.1
sqlparse==0.5.2

# External API resilience (cache-aside + retry + rate-limit, see DATASOURCE_RELIABILITY.md §4)
requests==2.32.5
tenacity==8.5.0

# API
django-filter==24.3
drf-spectacular==0.27.2

# Cache (optional)
django-redis==5.4.0

# Testing
pytest==8.3.3
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0

# Cheminformatics
rdkit

# Document processing & cheminformatics libraries (added: were missing after venv rebuild due to guarded imports)
django-unfold==0.96.0
python-docx==1.2.0
pubchempy==1.0.5
